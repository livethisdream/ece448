# Python virtual environments (dual-venv setup: .venv-linux for WSL, .venv-win for Windows).
.venv/
.venv-linux/
.venv-win/
env/
.env/
venv/

# Jupyter Book build output — regenerated by GitHub Actions on every push.
**/_build/

# Python bytecode caches.
__pycache__/
*.pyc

# Editor / OS cruft.
.vscode/
.idea/
.DS_Store
*.swp
*.bak

# Jupyter Book cache (kept out of version control; only useful locally).
**/.jupyter_cache/
